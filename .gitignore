# Credentials
.env

# Python venv
venv/
__pycache__/
*.pyc

# Runtime telemetry
# events.jsonl and detections_fallback.jsonl are produced by the running EDR.
*.jsonl
# Committed test fixtures are the exception.
!tests/data/*.jsonl
# Old-format files
kernel_events.json
execve_events.json

# Tests
.pytest_cache/

# Lab and evidence (phases 6-7)
lab/results/
quarantine/
edr.db
results/
