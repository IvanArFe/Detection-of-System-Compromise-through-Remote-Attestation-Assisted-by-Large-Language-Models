# Autonomous EDR — venv dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: bcc (python3-bpfcc) is a system package and does NOT install with pip.
# Install it with: sudo apt install python3-bpfcc
# Then create the symlinks into the venv if needed (see README.md, Installation).

mcp==1.27.0
requests==2.33.1
pytest==9.0.3
python-dotenv==1.2.2
supabase==2.29.0
